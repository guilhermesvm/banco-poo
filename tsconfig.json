{
  "compilerOptions": {
    "target": "ES6", // Define a versão do ECMAScript alvo
    "module": "commonjs", // Define o sistema de módulo
    "strict": true, // Habilita todas as verificações estritas
    "esModuleInterop": true, // Habilita compatibilidade com módulos ES6
    "skipLibCheck": true, // Pula a checagem de tipo de arquivos de definição de biblioteca
    "forceConsistentCasingInFileNames": true, // Força o uso consistente de maiúsculas/minúsculas em nomes de arquivos
    "outDir": "./dist", // Define o diretório de saída para os arquivos compilados
    "rootDir": "./src", // Define o diretório raiz de entrada
    "strictPropertyInitialization": false // Desabilita a checagem de inicialização de propriedades, se necessário
  },
  "include": [
    "src/**/*" // Inclui todos os arquivos TypeScript no diretório src
  ],
  "exclude": [
    "node_modules" // Exclui o diretório node_modules
  ]
}
